rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{userId}/images/{allPaths=**} {
      allow read: if true; // Public read access for portfolio images
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
} 